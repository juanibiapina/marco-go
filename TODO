- recover from errors in order to show all errors in a file instead of just the first one
- IDEA: move evaluation of each expression to its type and add Eval to the Expr interface
- Add print function for Expr
